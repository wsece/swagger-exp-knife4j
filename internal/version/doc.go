// Package version stores version metadata injected at build time (internal constant layer).
//
// Version, GitHash, GoBuildEnv, and GoBuildTime are written during compilation via ldflags or Makefile.
package version
