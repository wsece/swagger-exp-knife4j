// Package models defines GORM models persisted to the database (data layer).
//
// SwaggerAPIRecord: one row per API probe, with request/response summary, failure flag, SimHash grouping, and PacketJSON capture.
package models
